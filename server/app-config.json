{
  "stack": "python_3_9",
  "command": "uvicorn main:app --host 0.0.0.0 --port $PORT",
  "buildPath": ".",
  "scripts": {
    "preserve": "pip install -r requirements.txt"
  },
  "env_variables": {
    "PYTHONUNBUFFERED": "1"
  }
}
